import React from 'react';
import { View, Text, ScrollView } from 'react-native';

const AboutApp = () => {
  return (
    <ScrollView>
      <Text>About App</Text>
    </ScrollView>
  );
};

export default AboutApp;
